{
  "name": "@joeman4ever/watson",
  "version": "0.1.0-phase0",
  "private": true,
  "type": "module",
  "description": "Watson \u2014 Workflow-Aware Testing, Simulation, Observation & Navigation. An independent runtime verifier that drives a running product like a user and returns exact-HEAD evidence.",
  "bin": {
    "watson": "./src/cli.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "verify": "node src/cli.mjs verify",
    "doctor": "node src/cli.mjs doctor",
    "reap": "node src/cli.mjs reap",
    "lint": "eslint .",
    "test": "node --test test/*.test.mjs",
    "check": "for f in src/*.mjs tools/*.mjs; do node --check \"$f\" || exit 1; done",
    "ci": "npm run lint && npm run check && npm test"
  },
  "dependencies": {
    "jose": "^5.10.0",
    "pg": "^8.13.1",
    "playwright": "1.49.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
